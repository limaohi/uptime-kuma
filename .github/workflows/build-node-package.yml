name: Build Uptime Kuma Node Package

on:
  workflow_dispatch:
  push:
    branches:
      - master
    tags:
      - "v*"

permissions:
  contents: write

env:
  NODE_VERSION: "20"

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Show project information
        shell: bash
        run: |
          set -euo pipefail
          node --version
          npm --version
          node -p "require('./package.json').version"
          node -e "console.log(JSON.stringify(require('./package.json').scripts, null, 2))"

      - name: Install all dependencies
        run: npm ci --no-audit

      - name: Build application
        shell: bash
        run: |
          set -euo pipefail

          if npm run | grep -qE '^  build'; then
            npm run build
          elif npm run | grep -qE '^  build:backend'; then
            npm run build:backend
          else
            echo "No build script was found."
            echo "Available scripts:"
            npm run
            exit 1
          fi

      - name: Download distribution files
        run: npm run download-dist

      - name: Verify build output
        shell: bash
        run: |
          set -euo pipefail

          test -f server/server.js
          test -d node_modules
          test -d dist
          test -n "$(find dist -type f -print -quit)"

          echo "server/server.js:"
          ls -lh server/server.js

          echo "dist size:"
          du -sh dist

          echo "dist files:"
          find dist -maxdepth 2 -type f | head -30

      - name: Test production entrypoint
        shell: bash
        run: |
          set +e
          timeout 30s node server/server.js
          code=$?
          set -e

          if [ "$code" -ne 124 ]; then
            echo "Application exited unexpectedly with code: $code"
            exit "$code"
          fi

          echo "Application stayed alive for 30 seconds."

      - name: Remove development dependencies
        run: npm prune --omit=dev

      - name: Verify production dependencies
        shell: bash
        run: |
          set -euo pipefail
          test -d node_modules
          test -f server/server.js
          test -d dist
          test -n "$(find dist -type f -print -quit)"

      - name: Prepare package
        shell: bash
        run: |
          set -euo pipefail

          VERSION="$(node -p "require('./package.json').version")"
          SHORT_SHA="${GITHUB_SHA::7}"
          PACKAGE_NAME="uptime-kuma-${VERSION}-node${NODE_VERSION}-${SHORT_SHA}"

          rm -rf package
          mkdir -p "package/${PACKAGE_NAME}/data"

          cp -a server "package/${PACKAGE_NAME}/"
          cp -a node_modules "package/${PACKAGE_NAME}/"
          cp -a dist "package/${PACKAGE_NAME}/"
          cp -a package.json "package/${PACKAGE_NAME}/"
          cp -a package-lock.json "package/${PACKAGE_NAME}/"

          for item in src public extra frontend; do
            if [ -e "${item}" ]; then
              cp -a "${item}" "package/${PACKAGE_NAME}/"
            fi
          done

          printf '%s\n' \
            "version=${VERSION}" \
            "node_major=${NODE_VERSION}" \
            "commit=${GITHUB_SHA}" \
            "built_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            > "package/${PACKAGE_NAME}/VERSION"

          printf '%s\n' \
            '#!/usr/bin/env bash' \
            'set -e' \
            'cd "$(dirname "$0")"' \
            'export NODE_ENV="${NODE_ENV:-production}"' \
            'export UPTIME_KUMA_IS_CONTAINER="${UPTIME_KUMA_IS_CONTAINER:-1}"' \
            'exec node server/server.js' \
            > "package/${PACKAGE_NAME}/start.sh"

          chmod +x "package/${PACKAGE_NAME}/start.sh"

          tar --owner=0 --group=0 --numeric-owner \
            -czf "${PACKAGE_NAME}.tar.gz" \
            -C package "${PACKAGE_NAME}"

          sha256sum "${PACKAGE_NAME}.tar.gz" \
            > "${PACKAGE_NAME}.tar.gz.sha256"

          echo "PACKAGE_NAME=${PACKAGE_NAME}" >> "$GITHUB_ENV"

      - name: Verify archive contents
        shell: bash
        run: |
          set -euo pipefail

          tar -tzf "${PACKAGE_NAME}.tar.gz" \
            | grep -E "/server/server\.js$"

          tar -tzf "${PACKAGE_NAME}.tar.gz" \
            | grep -E "/dist/"

          tar -tzf "${PACKAGE_NAME}.tar.gz" \
            | grep -E "/node_modules/"

          echo "Archive size:"
          ls -lh "${PACKAGE_NAME}.tar.gz" "${PACKAGE_NAME}.tar.gz.sha256"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v5
        with:
          name: uptime-kuma-node-package
          path: |
            *.tar.gz
            *.tar.gz.sha256
          if-no-files-found: error

      - name: Publish GitHub Release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            *.tar.gz
            *.tar.gz.sha256
